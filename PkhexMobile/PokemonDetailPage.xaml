<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:PkhexMobile"
             x:Class="PkhexMobile.PokemonDetailPage"
             Title="Details">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="10">
            <Label x:Name="TitleLabel" FontSize="22" FontAttributes="Bold" />

            <Label Text="Nickname" FontAttributes="Bold" Margin="0,10,0,0" />
            <Entry x:Name="NicknameEntry" />

            <Label Text="Level" FontAttributes="Bold" />
            <Entry x:Name="LevelEntry" Keyboard="Numeric" />

            <Button x:Name="SaveChangesBtn"
                    Text="Save Changes"
                    Clicked="OnSaveChangesClicked"
                    HorizontalOptions="Fill"
                    Margin="0,10,0,0" />

            <Label x:Name="SaveStatusLabel" TextColor="Gray" LineBreakMode="WordWrap" />

            <BoxView HeightRequest="1" BackgroundColor="LightGray" Margin="0,10" />

            <CollectionView x:Name="StatsList">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="local:StatRow">
                        <Grid ColumnDefinitions="140,*" Padding="0,4">
                            <Label Grid.Column="0" Text="{Binding Label}" FontAttributes="Bold" />
                            <Label Grid.Column="1" Text="{Binding Value}" />
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
