Holds a Pokémon sprite. Shows a neutral placeholder until `src` is set (production supplies real PNGs — never hand-draw sprites).

```jsx
<SpriteSlot size="lg" shiny src="/sprites/charizard.png" />
```
