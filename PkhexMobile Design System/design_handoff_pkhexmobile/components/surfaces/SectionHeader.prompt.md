Header for a detail-screen group. Uppercase caps-tracked label; optional count pill and collapse chevron.

```jsx
<SectionHeader title="Moves" count={4} collapsible open={open} onToggle={toggle}
  action={<IconButton label="Edit" size="sm"><PencilIcon/></IconButton>} />
```
